package pjatk;

import javax.validation.*;
import java.lang.annotation.*;
import javax.validation.ConstraintValidator;
import javax.validation.ConstraintValidatorContext;
/*
public class PriceValidator implements ConstraintValidator<prodPrice, Integer> {
    private Integer validVar;

    @Override
    public boolean isValid(Integer prodPrice, ConstraintValidatorContext constraintValidatorContext) {

        while (!prodPrice.equals(null)) ;
        return true;
    }

    @Override
    public void initialize(prodPrice constraintAnnotation) {
        Integer validVar = constraintAnnotation.key();

    }
}
*/